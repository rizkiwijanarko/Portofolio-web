---
import '../styles/global.css';
import SiteHeader from '../components/SiteHeader.astro';
import SiteFooter from '../components/SiteFooter.astro';

interface Props {
  title: string;
  description?: string;
  image?: string;
  active?: string;
}

const { title, description, image, active } = Astro.props;
const siteTitle = 'Kharisma Rizki Wijanarko — AI Engineer';
const base = import.meta.env.BASE_URL;
const canonical = new URL(Astro.url.pathname, Astro.site);
const siteUrl = new URL(base, Astro.site);
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href={base + 'favicon.svg'} />
    <link rel="icon" href={base + 'favicon.ico'} />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description ?? siteTitle} />
    <link rel="canonical" href={canonical} />
    <meta property="og:title" content={title + ' — ' + siteTitle} />
    <meta property="og:description" content={description ?? siteTitle} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonical} />
    <meta property="og:site_name" content="Kharisma Rizki Wijanarko" />
    <meta name="twitter:card" content={image ? 'summary_large_image' : 'summary'} />
    <meta name="twitter:title" content={title + ' — ' + siteTitle} />
    <meta name="twitter:description" content={description ?? siteTitle} />
    {image && <meta property="og:image" content={new URL(image, Astro.site ?? Astro.url).toString()} />}
    {image && <meta property="og:image:alt" content={title + ' preview'} />}
    <script type="application/ld+json" set:html={JSON.stringify({
      '@context': 'https://schema.org',
      '@type': 'Person',
      name: 'Kharisma Rizki Wijanarko',
      jobTitle: 'AI Engineer',
      url: siteUrl.toString(),
      sameAs: [
        'https://github.com/rizkiwijanarko',
        'https://linkedin.com/in/rizki-wijanarko',
      ],
    })} />
    <title>{title} — {siteTitle}</title>
  </head>
  <body id="top">
    <a class="skip-link" href="#main">Skip to content</a>
    <SiteHeader active={active} />
    <main id="main">
      <slot />
    </main>
    <SiteFooter />
  </body>
</html>
